CREATE S3 BUCKET:

1. Go to AWS Management Console → S3 → Create Bucket.
2. Bucket Name: e.g., student-performance-datasets
3. Region: Choose your preferred region.
4. Block Public Access: Keep default (recommended).
5. Click Create Bucket.
6. Create a folder inside s3 bucket, give it any name and while uploading 
  upload the files to that folder.

CREATE LAMBDA FUNCTION:

1. Go to AWS Management Console → Lambda → Create Function.
2. Function Name: Ex: ProcessStudentData
3. Runtime: Python 3.x
4. Permissions: Create a new role with basic Lambda permissions and add DynamoDB & S3 access.
5. Attach policies: AmazonS3ReadOnlyAccess and AmazonDynamoDBFullAccess
6. Add lambda code in the code section.
7. Add S3 Trigger to Lambda
8. In your Lambda function → Configuration → Triggers → Add Trigger.
9. Select S3.
10. Bucket: student-performance-datasets
11. prefix: give the folder name ex:raw.
12. Event Type: All object create events

Click Add.